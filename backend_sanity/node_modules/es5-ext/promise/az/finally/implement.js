"use strict";

if (!require("es5-ext/promise/az/finally/is-implemented")()) {
	Object.defineProperty(Promise.prototype, "finally", {
		value: require("es5-ext/promise/az/finally/shim"),
		configurable: true,
		enumerable: false,
		writable: true
	});
}
